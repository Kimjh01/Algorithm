#include<iostream>
#include<cmath>

using namespace std;

int main(){
    ios::sync_with_stdio(0); 
    cin.tie(0);
	long long N;
   	cin >> N;
    cout << N*(N+1)/2<< "\n";
    cout << (N*(N+1)/2)*(N*(N+1)/2) << "\n";
    cout << (N*(N+1)/2)*(N*(N+1)/2);
    return 0;    
}