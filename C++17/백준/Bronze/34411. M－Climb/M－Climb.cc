#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    long long n, m, year;
    if (!(cin >> n)) 
        return 0;
    cin >> m >> year;
    cout << n * m;
    return 0;
}
